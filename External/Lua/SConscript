Import("outerEnv")

env = outerEnv.Clone()

env.AppendUnique(LIBS=["readline"], CPPDEFINES={'LUA_USE_LINUX' : '1'}, LINKFLAGS=["-install_name", "@executable_path/liblua.dylib"])
liblua = env.StaticLibrary("lua", Glob("src/*.c"))

Return("liblua")